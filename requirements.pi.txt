# Django
Django>=5.1,<5.2
djangorestframework>=3.15,<4.0
django-cors-headers>=4.3,<5.0
django-celery-beat>=2.6,<3.0
gunicorn>=22.0,<23.0

# Celery + Redis
celery>=5.4,<6.0
redis>=5.0,<6.0

# Binance
python-binance>=1.0.19
websockets>=12.0,<13.0
aiohttp>=3.9,<4.0

# Data analysis (no TA-Lib — not used in codebase)
numpy>=1.26,<2.0
pandas>=2.2,<3.0

# LLM providers — system uses whichever key is set (Anthropic preferred)
anthropic>=0.40,<1.0
openai>=1.50,<2.0

# Agno — optional, falls back to anthropic SDK
agno>=1.0

# Utilities
python-decouple>=3.8,<4.0
requests>=2.31,<3.0
asgiref>=3.7,<4.0
